优化要点：
参数化规则生成（新房首选，90% 场景用这个）
后端算法批量生成全部房号。
前端填入：
表格
参数 示例
楼栋编号 1# 楼
单元数量 2 单元
地上起始楼层 1 层
地上总层数 33 层
每层户数 4 户
每层户号序列 01,02,03,04
用户填完参数，后端传回生成的房号列表，用户先做预览：页面展示即将生成的一批房号 1‑1‑101、1‑1‑102 ... 1‑1‑3304；1‑2‑101 ...1‑2‑3304。
预览非常关键，防止参数填错，一次性生成几万条脏数据。用户确认无误，后端批量入库。
支持差异化配置：
不同单元层数不一样：单元 1 共 33 层，单元 2 共 28 层，可以分开设置；
地下层（负 1、负 2 储藏室 / 车位）单独一套生成逻辑，不和住宅混在一起；
支持自定义房号拼接规则。
后端实现要点：
使用批量bulk_insert，不要循环单条 insert，一栋楼几千户要快速落库；
数据库唯一约束：楼盘ID+楼栋+单元+房号防重复；
2、model层的SaleProject(楼盘) → SaleBuilding(楼栋) → SaleUnit(单元) → SaleHouse(房源)，存在几个业务逻辑缺陷、索引隐患、字段放置不合理，要适配参数化自动生成，需要做少量调整。现实新房：同一楼栋不同单元层数可以不一样，例如 1# 楼 1 单元 33 层，2 单元 27 层。楼栋上存总层数就会失真。
✅处理：楼栋保留total_floors仅做页面展示统计，真实生成房源的层数下沉到 SaleUnit（单元表）。
