将租户用户超级管理员权限页面html代码添加到项目frontend中
一、列表页.html(根据你的命名规范命名)
页面布局
---顶部标题
---搜索框（根据用户名搜索）
---回显区域（显示用户列表）---tab标签（姓名、手机号、角色、状态、详情）

---操作按钮---创建用户
---管理角色

1、顶部标签：{tenant_name}管理员操作页面，根据tenant_id动态获取tenant_name
2、搜索框：根据用户名搜索用户
3、回显区域：显示用户列表，每个用户显示姓名、手机号、角色、状态、详情按钮（点击后弹出用户详情页面）
4、操作按钮：创建用户、管理角色
5、如果用户列表为空，显示“暂无用户”
6、如果用户列表有数据，根据tab标签切换显示用户信息

二、用户详情页.html(根据你的命名规范命名)
页面布局
---返回按钮（点击后返回列表页）
---顶部标题
---用户信息（姓名、手机号、角色、状态）参考admin文件下的router层接口以及model/sys_user.py中的字段
---操作按钮---编辑用户信息（点击后弹出编辑用户信息弹窗）
---删除用户（点击后提示确认删除）
---重置密码（点击后提示确认重置密码）

三、创建用户页.html(根据你的命名规范命名)
页面布局
---返回按钮（点击后返回列表页）
---顶部标题
---用户信息（姓名、手机号、角色、状态）参考admin文件下的router层接口以及model/sys_user.py中的字段
---操作按钮---创建用户（点击后创建用户，成功后返回列表页）
---取消创建（点击后返回列表页）

四、管理角色页.html(根据你的命名规范命名)，参考admin文件下的router层接口以及model/sys_role.py中的字段
页面布局
---返回按钮（点击后返回列表页）
---顶部标题
---角色信息（角色名称、角色描述）
---操作按钮---创建角色（点击后创建角色）
---更新角色（点击后更新角色）
---删除角色（点击后提示确认删除）
如果角色列表为空，显示“暂无角色”，
如果角色列表有数据，根据tab标签切换显示角色信息
操作按钮：创建角色、更新角色、删除角色
